services:
  jellyrin:
    environment:
      JELLYRIN_PROVIDER_SECRET_KEYRING_FILE: /run/secrets/jellyrin-provider-keyring.json
    volumes:
      - ${JELLYRIN_PROVIDER_SECRET_KEYRING_HOST_FILE:?set an absolute root:10001 mode 0440 keyring path}:/run/secrets/jellyrin-provider-keyring.json:ro
